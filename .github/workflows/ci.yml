name: CI

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

permissions:
  contents: read

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

jobs:
  validate:
    name: ${{ matrix.os }} · Node ${{ matrix.node-version }}
    runs-on: ${{ matrix.os }}
    timeout-minutes: 20
    strategy:
      fail-fast: false
      matrix:
        os:
          - ubuntu-latest
          - macos-latest
        node-version:
          - "22.x"
          - "24.x"

    steps:
      - name: Check out the repository
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
          fetch-depth: 0

      - name: Set up Node.js
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: ${{ matrix.node-version }}
          cache: npm

      - name: Install dependencies
        run: npm ci --ignore-scripts

      - name: Check formatting
        run: npm run format:check

      - name: Lint
        run: npm run lint

      - name: Type-check
        run: npm run typecheck

      - name: Validate support matrix
        run: npm run support:validate

      - name: Validate upgrade policy
        run: npm run upgrade:validate

      - name: Validate release compatibility matrix
        run: npm run release-compatibility:validate

      - name: Validate policy contract
        run: npm run policy:validate

      - name: Validate policy and decision drift evaluation
        run: npm run policy-drift:validate

      - name: Validate adapter contract
        run: npm run adapter:validate

      - name: Validate Action fixture
        run: npm run action:validate

      - name: Validate benchmark baseline
        run: npm run benchmark:validate

      - name: Validate benchmark budgets
        run: npm run benchmark:budgets:validate

      - name: Validate bounded property and security corpus
        run: npm run property:validate

      - name: Run correctness, determinism, and performance gate
        run: npm run benchmark:ci

      - name: Validate release change-control register
        run: npm run change-control:validate

      - name: Test
        run: npm run test:coverage

      - name: Validate roadmap
        run: npm run roadmap:validate && npm run roadmap:test

      - name: Build
        run: npm run build

      - name: Validate identity regression corpus
        run: npm run identity:validate

      - name: Validate identity portability
        run: npm run identity:portability:validate

      - name: Validate identity quality gate
        run: npm run identity:quality:validate

      - name: Validate compatibility review
        run: npm run compatibility:validate

      - name: Validate documented workflow
        run: npm run workflow:validate

      - name: Verify package contents
        run: npm pack --dry-run --ignore-scripts --json

      - name: Smoke-test the installed package
        run:
          | # zizmor: ignore[adhoc-packages] Fresh local tarball consumer smoke; lifecycle scripts stay disabled.
          npm pack --ignore-scripts --pack-destination "$RUNNER_TEMP"
          mkdir "$RUNNER_TEMP/cartograph-consumer"
          cd "$RUNNER_TEMP/cartograph-consumer"
          npm init -y
          npm install --ignore-scripts "$RUNNER_TEMP/cartograph-cli-0.1.0.tgz"
          ./node_modules/.bin/cartograph --version
          ./node_modules/.bin/cartograph scan "$GITHUB_WORKSPACE/test/fixtures/typescript-express" > snapshot.json
          node -e 'const fs = require("node:fs"); const value = JSON.parse(fs.readFileSync("snapshot.json", "utf8")); if (value.schemaVersion !== 1 || value.edges.length === 0) process.exit(1)'
