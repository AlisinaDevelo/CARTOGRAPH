import { ambiguousValue } from "#ambiguous";
import { internalValue } from "#internal";
import { publicValue } from "cartograph-package-fixture/public";

export const values = [publicValue, internalValue, ambiguousValue];
